package application;

import application.domain.*;
import application.domain.Guest;

public class Main {

    public static void main(String[] args) {

        /*Person person = new Person("jd@med.com");

        person.setId(1);
        person.setName("John");
        person.setLastName("Doe");
        //person.setEmail("jd@mail.com");
        person.setPassword("123456");
        person.setState(true);

        System.out.println(person.getId());
        System.out.println(person.getName());
        System.out.println(person.getEmail());*/
/*
        //crear objeto
        BedRoom bedRoom = new BedRoom();
        bedRoom.createBedroon(bedRoom);
        bedRoom.getBedRoomById(1,bedRoom);*/

        /*//Person
        Person person = new Person();
        person.createUser(person);//CREAR USUARIO
        person.getUserById(2, person);//BUSCAR POR ID
        person.deleteUser(2, person); // ELIMINAR
        person.updateUser(2, person);// ACTUALIZAR
        person.getUserById(2, person);//BUSCAR POR ID*/

       /* //guest
        Guest guest = new Guest();
        guest.createGuest(guest);
        guest.getGuestById(1 , guest);
        guest.updateGuest(1, guest);
        guest.getGuestById(1, guest);*/

     /*   // EMPLOYE
        Employe employe = new Employe();
        employe.createEmploye(employe);
        employe.getEmployeById(1, employe);
        employe.updateEmploye(1,employe);
        employe.updateEmploye(1, employe);
        employe.deleteEmploye(1, employe);*/

        /*// BOOKING
        Booking booking = new Booking();
        booking.createBooking(booking);
        booking.getBookingById(1, booking);
        booking.updateBooking(1,booking);
        booking.getBookingById(1, booking);
        booking.deleteBooking(1, booking);*/

        /*// SERVICES
        Services services = new Services();
        services.createService(services);
        services.getServicesById(1, services);
        services.updateServices(1,services);
        services.getServicesById(1,services);
        services.deleteServices(1,services);*/
    }
}
